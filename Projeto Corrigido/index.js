let campoUser = $("user");
let campoSenha = $("pass");

$("form").submit(function (e) {
    e.preventDefault();
    if ($(campoUser).val() === "") {
    }
    if ($(campoSenha).val() === "") {
    }
});
